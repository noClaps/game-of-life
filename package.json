{
  "name": "conway",
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.1.13"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run build && bunx serve -n dist"
  }
}
