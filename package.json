{
  "name": "conway",
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.2.10",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "bunx vite build src --outDir ../dist --emptyOutDir",
    "dev": "bunx vite src"
  }
}
